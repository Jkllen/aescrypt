<Window x:Class="aescrypt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AES Encryption Tool"
        Height="550" Width="850"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.3" BlurRadius="6"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.85"/>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FFFF" BlurRadius="12" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="AES Encryption / Decryption"
                   FontSize="28"
                   FontWeight="Bold"
                   Foreground="#00FFFF"
                   HorizontalAlignment="Center"
                   Margin="0,10"/>

        <TextBox x:Name="InputTextBox"
                 Grid.Row="1"
                 Margin="0,25,0,10"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Height="120"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14"
                 Foreground="White"
                 Background="#2E2E3E"
                 BorderThickness="0"
                 Padding="10"
                 Text="Enter your paragraph (plaintext or encrypted text)..."
                 GotFocus="InputTextBox_GotFocus"
                 LostFocus="InputTextBox_LostFocus"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Vertical" HorizontalAlignment="Center" Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="EncryptButton"
                        Content="Encrypt"
                        Width="140"
                        Height="40"
                        Background="#00BFFF"
                        Click="EncryptButton_Click"/>
                <Button x:Name="DecryptButton"
                        Content="Decrypt"
                        Width="140"
                        Height="40"
                        Background="#32CD32"
                        Click="DecryptButton_Click"/>
            </StackPanel>

            <Button x:Name="SetFolderButton"
                    Content="Set Output Folder"
                    Width="220"
                    Height="38"
                    Background="#FF8C00"
                    Click="SetFolderButton_Click"/>

            <TextBlock x:Name="OutputFolderText"
                       Text="Current Output Folder: (default app directory)"
                       Foreground="LightGray"
                       FontSize="12"
                       TextWrapping="Wrap"
                       Margin="0,6,0,0"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Output TextBox -->
        <TextBox x:Name="OutputTextBox"
                 Grid.Row="3"
                 Margin="0,10,0,10"
                 Height="120"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14"
                 Background="#2E2E3E"
                 Foreground="White"
                 BorderThickness="0"
                 Padding="10"
                 Text="Output will appear here..."/>

        <!-- Status & File Path -->
        <StackPanel Grid.Row="4" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock Text="Last Saved File:"
                       Foreground="LightGray"
                       FontSize="13"
                       FontWeight="Bold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="LastSavedPathBox"
                     IsReadOnly="True"
                     Background="#2E2E3E"
                     Foreground="#00FFFF"
                     FontSize="12"
                     BorderThickness="0"
                     Padding="4,2"
                     Text="(no file saved yet)"/>
            <TextBlock x:Name="StatusText"
                       VerticalAlignment="Bottom"
                       HorizontalAlignment="Center"
                       Foreground="LightGray"
                       FontStyle="Italic"
                       Margin="0,5,0,0"
                       FontSize="12"/>
        </StackPanel>
    </Grid>
</Window>
